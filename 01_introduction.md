# Lesson 1: Introduction to JavaScript

## What is JavaScript?

JavaScript is a versatile programming language that runs in web browsers, servers, and many other environments. It’s one of the core technologies of the web, alongside HTML and CSS.

## Your First JavaScript Program

JavaScript can be written directly in HTML files or in separate `.js` files. Here’s your first program:

```javascript
console.log("Hello, World!");
```

This code prints “Hello, World!” to the browser’s console.

## Variables and Data Types

Variables store data that can be used and changed throughout your program.

### Declaring Variables

```javascript
let name = "Alice";        // Can be reassigned
const age = 25;            // Cannot be reassigned
var city = "New York";     // Older way (avoid in modern code)
```

### Basic Data Types

```javascript
// String - text data
let greeting = "Hello";

// Number - integers and decimals
let score = 100;
let price = 19.99;

// Boolean - true or false
let isActive = true;

// Undefined - variable declared but not assigned
let nothing;

// Null - intentional absence of value
let empty = null;
```

## Practice Exercises

1. Create a variable with your name and log it to the console
1. Create variables for your age, favorite color, and whether you like pizza (boolean)
1. Try changing a `const` variable and observe what happens

## Next Lesson

In Lesson 2, we’ll explore operators and how to perform calculations and comparisons in JavaScript.
